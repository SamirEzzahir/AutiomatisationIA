# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    container_name: formation-app
    ports:
      - '3000:5000'
    environment:
      - APP_VERSION=1.0
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
